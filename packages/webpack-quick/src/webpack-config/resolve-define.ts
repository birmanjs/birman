const prefixRE = /^BIRMAN_APP_/;

interface Options {

}

const ENV_SHOULD_PASS = ['NODE_ENV', 'HMR', 'SOCKET_SERVER', 'ERROR_OVERLAY'];

const resolveDefine = (opts: Options) => {

}

export default resolveDefine;
